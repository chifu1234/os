arch: amd64
os: linux
dist: focal
install: travis_wait 120
vm:
  size: x-large
script:
      - travis_wait 120 ./.scripts/ci.sh
addons:
  artifacts:
    # ⋮
    paths:
    -  ~/chromiumos/src/build/images/amd64-generic/latest/chromiumos_test_image.bin